<%- include ("../partials/navbar") %>
<div>
    <h1>Users in DB</h1>
    <a href="/logout">Log out</a>

    <ul>

        <% for (let i = 0; i < users.length; i++) { %>
            <li>
                <%= users[i].name %> -
                    <%= users[i].email %>
            </li>
            <% } %>
    </ul>

</div>